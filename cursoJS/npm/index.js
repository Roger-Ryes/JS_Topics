//npm (Node Package Manager)
const colors = require('colors');

console.log("hello coders".green);

/*para instalar una lista de paquetes que se instale en nuestro proyecto se usa el comando:
    $npm init
Posteriormente lo que hara es pedir informacion del proyecto como nombre, version, etc

Para iniciar npm y que empiece a leer el "package.json" se ejecuta el comando:
    $npm start

Scrip de package
    start es lo que ejecutar luego de insertar el comando "npm start".
    Si se desea ejecutar uno creado por nosotros (ejm:desarrollo) se ejecuta el comando
    
    $npm run desarrollo
*/ 
